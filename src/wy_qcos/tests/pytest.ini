[pytest]
# 用例标记
markers =
    smoke: 冒烟测试用例（仅执行核心测试）
    slow: 耗时较长的测试用例
    default: 默认用例（非smoke, 非slow用例）
    all: 全量用例

# 配置全局默认参数
# 可组合多个参数，空格分隔即可
addopts = --disable-warnings -vv --log-cli-level=INFO
