[pytest]
# pytest markers
markers =
    smoke: smoke testcases
    slow: slow testcases
    driver: driver testcases
    default: default testcases (non-smoke, non-slow)

# default options
addopts = --disable-warnings -vv --log-cli-level=INFO
